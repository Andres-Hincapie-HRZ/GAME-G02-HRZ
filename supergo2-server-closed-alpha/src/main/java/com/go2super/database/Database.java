// Declaración del paquete donde se encuentra esta clase
package com.go2super.database;

// Importación de la anotación SneakyThrows de Lombok
import lombok.SneakyThrows;

// Declaración de la clase Database
public class Database {

    // Anotación que permite lanzar excepciones checked como unchecked
    @SneakyThrows
    // Método estático público para configurar la base de datos
    public static void setup() {
        // Cuerpo vacío del método (la implementación está pendiente)
    }

}
